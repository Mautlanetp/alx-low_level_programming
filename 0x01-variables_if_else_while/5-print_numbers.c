#include <stdio.h>

/**
 * main - prints single digit numbers
 *
 * Return: Always 0(seccess)
 */

int main(void)
{
	int i = 0;

	for (i = 0; i < 10; i++)
	{
		printf("%d", i);

	}

	putchar('\n');

	return (0);
}

